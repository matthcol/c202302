#include <stdio.h>

// #include "mytypes.h"
#include "playground.h"


// int main(){
// int main(int argc, char *argv[]){ 
int main(int argc, char *argv[], char *arge[]){     
    // printf("App started with %d args:\n", argc);
    // for (int i=0; i < argc; i++) {
    //     printf("\t[%d] = %s\n", i, argv[i]);
    // }
    // printf("Environment:\n");
    // for (; *arge != NULL; arge++) {
    // // for (int i=0; i < 6; i++) {
    //     printf("\t%s\n", *arge);
    // }
    // playWithInts();
    // playWithDoubles();
    // bool ok = isWeatherOk();
    // playWithStrings();
    // playWithSwap();

    playWithFind();   
    return 0;
}